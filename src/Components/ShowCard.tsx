// components/ShowCard.js
import React from "react";

export default function ShowCard({ name, releaseDate, ratings, image, delay }) {
  return (
    <div>
      {/* Card display */}
    </div>
  );
}
